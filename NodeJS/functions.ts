//number,string,boolean
function sample(a:number,b:number):number{ //(variable:datatype of the variable):datatype of return value
    return a+b;
}
console.log(sample(10,20))
//to run --> tsc filename.extension
//after successful compilation, js file gets created automatically. 
//Then run js file.